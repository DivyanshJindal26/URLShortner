services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8111:3000"
    env_file: ./server/.env
    volumes:
      - uploads:/app/uploads

volumes:
  uploads:
